---
description: Gai framework source code — conventions for developing the framework itself or understanding it.
globs: "**/*.go"
alwaysApply: true
---

# Gai Framework — 框架源码开发指南

## 仓库性质

这是 **Gai 框架的源码仓库**（`github.com/Hlgxz/gai`）。Gai 是一个 Go Web 框架**库**，用户通过以下方式使用：

```bash
# 安装 CLI
go install github.com/Hlgxz/gai/cmd/gai@latest

# 创建业务项目（自动包含全平台 AI 规则文件）
gai new myapp --module github.com/user/myapp
cd myapp && go mod tidy && gai serve
```

`gai new` 生成的项目会自动包含 `.cursor/rules/`、`CLAUDE.md`、`AGENTS.md` 等所有 AI 工具的规则文件。

## 框架开发环境

```bash
go mod tidy && go build ./... && go vet ./...
```

## 代码结构

```
gai/                    → 根包: Application, Container, ServiceProvider
├── cmd/gai/            → CLI (new/serve/make/generate/migrate)
├── config/             → YAML + .env 配置
├── router/             → HTTP 路由 (Group, :param, Resource)
├── http/               → Context + Validator
├── database/orm/       → 泛型 ORM: Query[T], Get[T], Create[T]
├── database/driver/    → MySQL, PostgreSQL, SQLite 驱动
├── database/migration/ → 迁移引擎 + Blueprint
├── auth/               → 多 Guard 认证 + JWT
├── miniapp/wechat/     → 微信 SDK (登录/支付/消息)
├── miniapp/alipay/     → 支付宝 SDK
├── ai/schema/          → YAML Schema 解析
├── ai/generator/       → 代码生成 (Model/Controller/Migration/Routes)
├── middleware/          → CORS, Logger, Recovery, RateLimit
└── support/            → Snake, Camel, Plural, Hash, Env
```

## 核心约定

1. `github.com/Hlgxz/gai/http` **必须**用 `ghttp` 别名
2. Handler: `func(c *ghttp.Context)`，用 `c.Success()`/`c.Error()`
3. ORM 泛型函数: `orm.Query[T]()`, `orm.Get[T]()`, `orm.Create[T]()`
4. Model 嵌入 `orm.Model`，`gai:"..."` 标签
5. 中间件必须调用 `c.Next()`
6. 文件 snake_case，表名自动 plural snake_case
7. 不在 `database/driver/` 之外使用 `init()`

## 修改框架时

| 场景 | 修改位置 |
|------|---------|
| 新 Schema 类型 | `ai/schema/types.go` + 3 个 driver 的 ColumnType |
| 新 CLI 命令 | `cmd/gai/` + `main.go` 注册 |
| 新内置中间件 | `middleware/` |
| 新 Guard 类型 | 实现 `auth.Guard` 接口 |
| 新 DB 驱动 | 实现 `driver.Driver`，init() 中 Register |
| 修改生成模板 | `ai/generator/*.go` 中的 const xxxTemplate |
