---
description: How to use the Gai framework to create business projects.
globs: "cmd/**/*.go"
---

# Gai 框架使用指南（面向 AI Agent）

## 当用户说"帮我用 Gai 开发一个项目"

执行以下步骤：

```bash
# 1. 安装 Gai CLI
go install github.com/Hlgxz/gai/cmd/gai@latest

# 2. 创建项目（自动包含 AI 规则文件）
gai new <项目名> --module <module路径> --port 8080

# 3. 进入项目并安装依赖
cd <项目名>
go mod tidy

# 4. 启动开发服务器
gai serve
```

## 添加业务功能

### Schema 驱动（推荐）
1. 在 `schemas/` 中创建 YAML:
```yaml
model: Product
fields:
  - name: name
    type: string
    size: 200
    rules: required|min:1
  - name: price
    type: decimal
    rules: required|numeric
api:
  prefix: /api/v1/products
  actions: [index, show, store, update, destroy]
  auth: jwt
```
2. `gai generate --schema schemas/product.yaml`
3. 在 `routes/routes.go` 中注册路由

### 手动创建
```bash
gai make model Product
gai make controller Product
gai make migration create_products_table
```

## 生成的项目结构

```
myapp/
├── app/controllers/    # 控制器
├── app/models/         # ORM 模型
├── app/middleware/      # 自定义中间件
├── config/app.yaml     # 配置
├── database/migrations/
├── routes/routes.go    # 路由注册
├── schemas/            # Schema YAML
├── .cursor/rules/      # Cursor AI 规则
├── CLAUDE.md           # Claude Code 规则
├── AGENTS.md           # Codex / 通用 Agent 规则
├── .github/copilot-instructions.md
├── .windsurfrules
├── .kiro/rules.md
├── .gemini/style-guide.md
├── .roo/rules.md
├── .clinerules
├── .augment/rules.md
├── main.go
└── go.mod
```

每个 AI 工具打开这个项目都能立即理解 Gai 框架的约定并正确编码。
